	
var x = document.getElementById("listePays");
listPaysMonde.forEach(function(d){
	var option = document.createElement("option");
	option.text = d;
	x.add(option);
});
  
x.onchange = function initMap(){
	

  d3.json("http://fxjollois.github.io/webreporting/departements.geojson", function(error, data) {
    if (error) throw error;

    // Ici, à chaque département, on lui associe les valeurs du chômage
    data.features.forEach(function(d) {
     
	 
	 
	 
	 
    });